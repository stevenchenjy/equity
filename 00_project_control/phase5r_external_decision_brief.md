# Phase 5R External Decision Brief

Date: 2026-07-27 ET

## Complete

- One canonical low-attention daily workflow is active and verified.
- Weekly/D1/D2/D3 workflows are retired and unloaded.
- Local model contracts, deterministic C9 authority, leakage controls,
  valuation/freshness gates, replay/evaluation mechanics, cost routing,
  fixed private ledger, provider-native cache metering, and fail-closed shadow
  boundaries pass.
- Full local suite: `350/350`.
- No provider call, paid data use, model email influence, broker access, order,
  or trade was performed.

## Cheapest credible next step

Run one frozen, public-evidence pilot:

- 10 packets;
- 10 Luna analyst Batch requests;
- the same 10 Terra analyst Batch requests;
- 10 Sol committee/critic Batch requests;
- at most 30 physical attempts;
- estimated cost `$2.89`;
- hard cap `$5.00`;
- no canonical or email effect.

Do not purchase licensed market data or a second model provider yet. Public
market context is enough for research-only shadow, and action-grade transitions
remain blocked. Buy either dependency only after the OpenAI replay proves a
specific need.

## Exact user decisions required

Please provide or approve all four items:

1. **SEC contact** — a project name and contact email for the SEC User-Agent.
   It will be passed at runtime and not committed.
2. **Pilot storage** — authorize up to `5 GB` for the first 10-packet corpus.
   Qualification storage will be reconsidered after actual pilot usage; the
   current typical full estimate is about `0.96 GB`, with a conservative
   planning upper bound of `61.35 GB`.
3. **Model pilot** — authorize public-input Batch processing with a maximum of
   `30` physical calls and `$5.00`.
4. **Independent review** — identify two independent reviewers, or confirm
   that you will be one reviewer and name a second reviewer. This is a
   one-time qualification task, not daily approval.

Separately configure OpenAI authentication outside the repository after
approving item 3. Do not paste an API key into chat, the repository, a log, or
a LaunchAgent.

After these decisions, Codex can acquire and freeze the pilot corpus, prepare
the review set, run the bounded provider pilot, verify actual token/USD
receipts, and either reject the model path or advance it to the larger
qualification gate.
