# Phase 5R External Decision Brief

Date: 2026-07-28 ET

## Complete

- One canonical low-attention daily workflow is active and verified.
- Weekly/D1/D2/D3 workflows are retired and unloaded.
- Local model contracts, deterministic C9 authority, leakage controls,
  valuation/freshness gates, replay/evaluation mechanics, cost routing,
  fixed private ledger, provider-native cache metering, and fail-closed shadow
  boundaries pass.
- Full local suite: `369/369`.
- No provider call, paid data use, model email influence, broker access, order,
  or trade was performed.
- SEC contact, 5 GB storage, and 30-call/$5 Pilot authorization were supplied.
- Ten real-source packets now pass both the narrow verifier and strict
  point-in-time inventory.
- A dedicated check-only/externally injected pilot runner now freezes exactly
  30 model-inference calls plus 30 non-inference token counts, reserves
  `$4.9368` worst case under the `$5` cap, disables SDK
  retries, locks the one production quarantine, enforces exact input counts,
  and produces randomly blinded human-review materials only after all calls
  succeed.
- The isolated Python 3.11.15/OpenAI SDK 2.49.0 runtime is installed and its
  full dependency lock is plan-bound; its offline boundary smoke test passed.

## Pilot result

**NO-GO pending external OpenAI authentication.**

The original `0/10` data-quality stop was repaired without weakening the gate.
Raw submissions, Company Facts, exact-accession XBRL reconciliation, and
filing-index-bound exhibit discovery now produce `10/10` strict completion.
The corpus uses `37,968,013` bytes.

Actual provider calls: `0/30`. Actual cost: `$0.00/$5.00`.
Read-only pilot readiness: PASS. Daily refresh and decision jobs remain loaded;
the model shadow scheduler is absent and unloaded.

## Remaining blockers

1. Make OpenAI authentication available to the isolated runtime outside the
   repository, then restart Codex and resume this task. Do not paste an API key
   into chat, source files, logs, or a LaunchAgent.
2. Run before the pinned pricing expires on 2026-08-04, or reverify official
   pricing and update the policy first.
3. Identify independent reviewers after quarantined model claims exist; human
   citation accuracy, unsupported-claim rate, and critic incremental value
   remain unmeasured until that review is completed.

The 30-call/$5 authorization remains entirely unused. Licensed data, a second
provider, Shadow scheduler installation, canonical influence, and email
influence remain disabled.
